import { HttpsFunction, CallableRequest } from 'firebase-functions/v2/https';

// We can remove our custom types and use the ones from firebase-functions/v2
export type { CallableRequest, HttpsFunction };
